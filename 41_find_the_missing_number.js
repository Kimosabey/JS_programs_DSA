// Find the missing number from unsorted array with O(n) complexity

let array = [2, 3, 7, 8, 5, 1, 4, 3, 6];

function Missing(arr) {
  // 1st sort the array

  let len = arr.length;
  let sum = 1;

  for (let i = 2; i < len + 1; i++) {
    const element = array[i];
  }
}

console.log(Missing(array));
